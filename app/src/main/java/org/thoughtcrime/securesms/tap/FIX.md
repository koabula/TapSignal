切片1（1-2天）：修复加密边界（Integrator 接入加密产物）、修正 send/recv 元数据与路径规则、禁用或调整只读校验以统一与阶段策略。
切片2（2-3天）：Token 池安全存储迁移（读写双写/按版本开关），提供 Token 查询 API，移除反射。
切片3（2-4天）：Tap→Signal 入库链路打通（去重、幂等、错误处理），附最小 e2e 测试。
切片4（2天）：非 COS 健康检查兜底、路由权重配置化、Provider 侧 listFiles 过滤收敛。